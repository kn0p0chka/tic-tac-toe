import { GameConfig } from "../models/display";

const defaultConfig: GameConfig = {
  boardSize: 3,
  enableTurnDisappearing: false,
  winCombinationLength: 3,
  disappearingCellsInpVal: 5,
};

export default defaultConfig;
